[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live: makes real network calls to a live LLM provider (costs API credits); auto-skipped unless the relevant API key is set. Exclude explicitly with -m "not live".
